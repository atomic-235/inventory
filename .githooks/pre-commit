#!/usr/bin/env bash
set -euo pipefail

if ! command -v pnpm >/dev/null 2>&1; then
  echo "pre-commit: pnpm not found (not in direnv shell); skipping tests" >&2
  exit 0
fi

echo "pre-commit: running unit tests (pnpm test)"
pnpm test